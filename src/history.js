// import createHistory from 'history/createBrowserHistory';

// export default createHistory();

import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();
/*
  history: is a package was installed automatically with react-router-dom
  createBrowserHistory: we are importing from the history package a FIle called createBrowserHistory
  SO
  createHistory is a function that we can call to create a new history object
*/